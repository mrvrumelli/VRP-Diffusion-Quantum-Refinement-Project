experiment_name: gat_pretrain
seed: 42
alignment:
  track: ours_robust
  contract_version: 1
  paper_id: arxiv:2603.07568v1
  claim: project extension with audited-label and robustness choices; not a paper baseline
dataset:
  name: cvrp_uniform_train
  # Template: set this to a materialized training split, or use gat_pretrain_s7799.yaml locally.
  path: null
validation:
  path: null
output:
  root: outputs/train
model:
  hidden_dim: 192
  gat_num_layers: 5
  gat_num_heads: 8
  dropout: 0.0
training:
  epochs: 40
  learning_rate: 0.001
  batch_size: 16
  device: auto
  augmentation: false
  weighted_bce: true
  pos_weight_power: 0.5
  same_size_batches: true
  adaptive_threshold: true
  early_stop_patience: 5
mlflow:
  enabled: true
